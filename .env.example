# Copy to .env and fill in. Baked into the build (import.meta.env.WXT_*).
# Both are LOW-VALUE / client-shippable — never put the real OpenAI key here.
WXT_PROXY_URL=https://mood-scroll.vercel.app/api/openai
WXT_APP_TOKEN=replace-with-a-random-low-value-token
